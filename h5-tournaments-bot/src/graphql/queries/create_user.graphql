mutation CreateUserMutation($name: String!, $discordId: Int!, $discordNick: String!) {
    createUser(name: $name, discordId: $discordId, discordNick: $discordNick) {
        id,
        nickname,
        registered,
        discordId
    }
} 