mutation UpdateMatchMutation($id: UUID!, $gamesCount: Int, $secondPlayer: UUID, $dataMessage: String) {
    updateMatch(id: $id, gamesCount: $gamesCount, secondPlayer: $secondPlayer, dataMessage: $dataMessage)
}