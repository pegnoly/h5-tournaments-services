mutation UpdateMatchMutation($id: UUID!, $gamesCount: Int, $secondPlayer: UUID, $dataMessage: String, $currentGame: Int) {
    updateMatch(id: $id, gamesCount: $gamesCount, secondPlayer: $secondPlayer, dataMessage: $dataMessage, currentGame: $currentGame)
}