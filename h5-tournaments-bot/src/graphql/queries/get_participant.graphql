query GetParticipant($tournamentId: UUID, $userId: UUID, $challonge: String) {
    participant(tournamentId: $tournamentId, userId: $userId, challonge: $challonge) {
        id,
        user,
        challonge
    }
}